---
// BannerNosotros.astro — Static Hero Parallax (Liquid Glass Pro)
---

<section
  class="relative w-full h-[65vh] min-h-125 bg-slate-950 overflow-hidden select-none"
  id="nosotros-banner"
>
  <div
    class="absolute inset-0 w-full h-[130%] top-[-15%] pointer-events-none overflow-hidden bg-slate-950"
  >
    <img
      src="/nosotros/nostros_1.webp"
      alt="Nuestras Instalaciones Chickenbaby"
      loading="eager"
      class="parallax-bg-img w-full h-full object-cover object-center opacity-0 scale-105 in-[.is-loaded]:opacity-70 in-[.is-loaded]:scale-100 transition-all duration-[2s] ease-[cubic-bezier(0.16,1,0.3,1)]"
    />
  </div>

  <div
    class="absolute inset-0 bg-linear-to-b from-slate-950/80 via-transparent to-slate-950/90 z-10 pointer-events-none"
  >
  </div>
  <div
    class="absolute inset-0 bg-linear-to-br from-sky-500/10 via-transparent to-amber-500/10 z-10 pointer-events-none mix-blend-overlay"
  >
  </div>

  <div
    class="relative z-20 w-full h-full flex items-center justify-center px-4 sm:px-6 mt-10"
  >
    <div
      id="banner-content"
      class="relative group max-w-4xl w-full p-8 md:p-14 rounded-[3rem] text-center overflow-hidden opacity-0 translate-y-12 in-[.is-loaded]:opacity-100 in-[.is-loaded]:translate-y-0 transition-all duration-1000 ease-[cubic-bezier(0.16,1,0.3,1)] delay-200
            bg-slate-900/40 backdrop-blur-3xl backdrop-saturate-150
            border border-white/10 shadow-[0_30px_60px_rgba(0,0,0,0.5)]"
    >
      <div
        class="absolute -inset-20 z-0 opacity-30 blur-3xl pointer-events-none rounded-[3rem] bg-linear-to-b from-sky-400/20 via-transparent to-amber-400/10 group-hover:opacity-50 transition-opacity duration-1000"
      >
      </div>

      <div class="relative z-10 flex flex-col items-center gap-6">
        <div
          class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-white/5 backdrop-blur-md border border-white/10 text-sky-400 text-[10px] md:text-xs font-black uppercase tracking-[0.2em] shadow-[0_4px_12px_rgba(0,0,0,0.2)]"
        >
          <span class="w-1.5 h-1.5 rounded-full bg-amber-400 animate-pulse"
          ></span>
          Sobre Nosotros
        </div>

        <h1
          class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black leading-[1.05] tracking-tighter text-white m-0 font-heading drop-shadow-md"
        >
          El origen de la <br class="hidden sm:block" />
          <span
            class="text-transparent bg-clip-text bg-linear-to-r from-sky-400 via-sky-200 to-white"
          >
            mejor genética.
          </span>
        </h1>

        <p
          class="text-slate-300 text-sm md:text-lg font-medium leading-relaxed max-w-2xl mx-auto drop-shadow-sm"
        >
          Más de 16 años dedicados a transformar la industria avícola con
          pasión, tecnología y un compromiso inquebrantable enfocados en la
          experiencia al cliente y en el desarrollo de la comunidad.
        </p>
      </div>
    </div>
  </div>

  <div
    class="absolute bottom-0 left-0 w-full h-0.5 bg-linear-to-r from-transparent via-sky-500/50 to-transparent z-30"
  >
  </div>
</section>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Nunito:wght@800;900&display=swap");

  .font-heading {
    font-family: "Nunito", sans-serif;
  }

  .parallax-bg-img {
    will-change: transform;
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const section = document.getElementById("nosotros-banner");
    if (!section) return;

    // 1. ANIMACIÓN DE CARGA INICIAL (Sin blur en textos para máxima nitidez)
    setTimeout(() => {
      section.classList.add("is-loaded");
    }, 100);

    // 2. PARALLAX EXCLUSIVO PARA LA IMAGEN (Atado al Scroll)
    const parallaxImages = document.querySelectorAll(
      ".parallax-bg-img",
    ) as NodeListOf<HTMLElement>;
    // También movemos ligeramente la tarjeta de cristal para un efecto 3D completo
    const bannerContent = document.getElementById(
      "banner-content",
    ) as HTMLElement | null;
    let ticking = false;

    window.addEventListener(
      "scroll",
      () => {
        if (!ticking) {
          window.requestAnimationFrame(() => {
            const scrollY = window.scrollY;
            const sectionHeight = section.offsetHeight;

            // Solo animamos si el banner está visible en la parte superior
            if (scrollY <= sectionHeight) {
              // La imagen de fondo baja lentamente (Crea profundidad)
              const imgOffset = scrollY * 0.4;
              parallaxImages.forEach((img) => {
                img.style.transform = `translate3d(0, ${imgOffset}px, 0)`;
              });

              // La tarjeta de cristal sube sutilmente
              if (bannerContent) {
                const cardOffset = -(scrollY * 0.15);
                bannerContent.style.transform = `translate3d(0, ${cardOffset}px, 0)`;
              }
            }

            ticking = false;
          });
          ticking = true;
        }
      },
      { passive: true },
    );
  });
</script>
